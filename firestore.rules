rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Ajusta aquí tu política real; esto deniega todo por defecto:
    match /{document=**} {
      allow read, write: if false;
    }
  }
}